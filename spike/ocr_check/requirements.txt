# EasyOCR은 뺐다. 같은 공고 이미지에서 평균 신뢰도 0.396, 3배로 키워도 안 고쳐졌다.
fastapi
uvicorn[standard]
python-multipart
pillow

# 기본 엔진 — 윈도우·맥·리눅스 전부에서 돈다
paddlepaddle
paddleocr

# macOS Vision 은 설치할 게 없다 (내장). vision_ocr.swift 를 swiftc 로 컴파일해 쓴다

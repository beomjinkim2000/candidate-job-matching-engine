# 지원자-공고 매칭 스코어링 엔진 (그룹바이 채용 과제)

> **이 파일은 작업 내내 곁에 두고 지킨 제약 목록이다.** 과제문을 읽고 「절대 어기면 안 되는 것」을
> 먼저 뽑아 고정해 둔 것이고, 코딩 도구가 매 작업마다 읽는 규칙 파일이기도 하다.
> 진행 상황은 `Plans.md` (Status 컬럼만 신뢰).

마감: 과제 수령 후 **24시간**.

---

## 절대 지켜야 할 것

### 데이터
- CRITICAL: 사람인(saramin.co.kr) 공고를 **API로 크롤링**해서 확보한다. **공고 내용이 이미지인 공고**를 고른다.
  - ⚠️ **API 키가 반려됐다.** 이 줄은 지우지 않고 남긴다 — **못 지킨 제약을 지운 것처럼 보이게 하지 않는다.**
    대신 어떻게 우회했는지는 `docs/SARAMIN_API.md` · `docs/IMAGE_ACQUISITION.md`에 적었다
- CRITICAL: **공고 원문 텍스트 복사·붙여넣기 금지.** 이미지에서 파싱한 결과만이 파이프라인의 입력이다.
  - 이 제약이 **검산 G4**(모든 조건에 이미지 좌표가 있는가)의 이유다. 좌표가 없으면 결과가 안 나간다
- CRITICAL: 공고 **2개, 서로 다른 직군**. 각 공고마다 목업 이력서 **6명** — 완벽 매칭 2 / 부분 매칭 3 / 미스매칭 1.

### 엔진
- CRITICAL: 점수는 **0~100점**, **랭킹으로 정렬**, **사람이 읽을 수 있는 근거**를 함께 낸다.
- CRITICAL: **직군 무관 일반화.** 특정 스킬셋·직군을 하드코딩하지 않는다. **주석에도 쓰지 않는다.**
- CLI 또는 API 엔드포인트 **하나**로 실행 가능해야 한다. 결과 확인용 **간단한 UI**를 만든다.
- CRITICAL: **법적 우회 구조는 구현하지 않는다.** 아키텍처로만 정리한다 (`docs/LEGAL_ARCHITECTURE.md`).

### 제출
- GitHub **public** 레포 (코드 + README + 목업 데이터셋 + 최소 테스트).
- CRITICAL: **`README.md`는 코딩 도구가 쓰지 않는다.** 초안도, 임시본도, "나중에 지우면 되는"
  버전도 만들지 않는다. README는 **지원자 본인이 직접 쓴다.** 도구가 하는 일은 `docs/`에
  **소재만** 쌓는 것뿐이다 (`docs/TRADEOFFS.md` 등). 다른 문서에서 README를 언급할 때도
  파일을 생성하지 않는다.
  - ⚠️ **「쓰지 않는다」와 「없어야 한다」는 다르다.** 과제 제출물은 **「코드 + README」**이고
    「설계 결정과 트레이드오프를 **반드시 서술**」하라고 했다 — **README가 없으면 그게 요구
    위반이다.** 판별은 `git log --format=%an -- README.md`의 **작성자**로 한다.
- 테스트는 커버리지가 아니라 **어떤 케이스를 골랐는지**가 평가 대상이다.
- **본인의 문제 정의와 판단**이 드러나야 한다. AI 산출물을 그대로 내지 않는다.

### 보안
- CRITICAL: OpenAI API 키는 **`.env`로만** 관리한다. 코드·문서·로그·화면 출력 어디에도 키 원문을 노출하지 않는다.
- CRITICAL: push 전 `git ls-files`로 키 파일 미포함을 확인한다. 템플릿은 `.env.example`만 싣는다.

---

## 기술 스택

Python 3.x / FastAPI (단일 진입점 `run.py`) + 정적 HTML UI · **PaddleOCR**(공고 이미지 → 줄·좌표) ·
OpenAI API(**텍스트만** — 헤더 역할 분류 · 조건 갈래 판정 · 판단 층 채점) · pytest / ruff

> **이미지를 LLM에 보내지 않는다.** 비전 모델은 좌표를 지어내고, 좌표가 근거 사슬의 끝이라
> 지어낸 좌표는 근거가 아니다. 좌표의 출처는 OCR 하나뿐이다 (ADR-005 개정).

## 작업 규칙

- 커밋 메시지는 conventional commits (`feat:` `fix:` `docs:` `refactor:`).
- 로컬 테스트는 **변경한 파일만** (`pytest tests/test_<대상>.py -q`). 전체 검증은 push 후 CI.
- 커밋 전 `ruff check <변경 파일>`.

---

## 참조

| 위치 | 내용 |
|---|---|
| `Plans.md` | 실행 계획 · **실행이 계획을 바꾼 곳** |
| `docs/CLAUDE.md` | 문서 지도 · 문헌 검토 결과 · 인용 시 주의사항 |
| `docs/ADR.md` | 결정 13개. **실측으로 뒤집힌 것은 「개정」으로 남긴다** |
| `docs/SARAMIN_API.md` | **사람인 API 사양과 우리가 쓰려던 범위.** 조건성 필드(`experience-level`·`required-education-level` 등)는 **읽지 않는다** — 원문 복붙 금지 |
| `docs/IMAGE_ACQUISITION.md` | **API는 이미지를 안 준다**(응답 필드 검증). 데모 4단계 중 3단계는 API 밖 |
| `docs/LEGAL_ARCHITECTURE.md` | 고객사 경유 데이터 구조 (**설계만, 구현 안 함**) |
| `src/CLAUDE.md` | 확정된 아키텍처와 스코어링 규칙 (**구현 전 필독**) |
| `tests/CLAUDE.md` | 테스트 케이스 선정 근거 |

### 외부 의존성 상태
- **사람인 API**: 즉시 발급 아님. 이용신청 → 승인 메일 → 앱 등록 → access-key.
  **신청이 반려됐다.** 그래서 API는 본문 파이프에서 빠지고 **대조군**(동일성 확인·상태감시·
  파싱 교차검증) 자리로 물러났다. `SaraminSource`는 코드만 있고 실행되지 않는다.

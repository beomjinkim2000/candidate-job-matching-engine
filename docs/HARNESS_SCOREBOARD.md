# 하네스 모의심사 — 라운드 이력

대상: `phases/matching-engine/step0~12.md` + `.claude/commands/harness.md` (81,398자)
방법: `plan-review` — 심사위원 8인 병렬. 계획문서 심사(「됐는가」가 아니라 **「되게 설계됐는가」**)

---

## R1 — 2026-09-01

루브릭: `RUBRIC_R1.md` · 봉인 해시 `1379831dd28ef5bf327d7cd5d720d0f5cbcdca7c466b9310715cf992e0c429ba`
(과제문에 공개 배점표가 없어 **역설계했다.** 배점은 주최측이 준 숫자가 아니다)

### 스코어카드 — 51 / 100

| 구분 | 항목 | 배점 | 점수 | 근거 |
|---|---|---|---|---|
| 축 | A 요구사항 충족 설계 | 12 | 7 | 요구 ①②가 실행 후에도 미판정으로 남는 구조 |
| 축 | B 채점 설계의 반증 가능성 | 14 | **7** | 원점수 8 → **G3 0점으로 50% 상한 적용** |
| 축 | C 24시간 실현성 | 14 | 8 | 비용·시간 숫자 0건 |
| 축 | D **본인의 판단** | 14 | 8 | docs는 최상급, 하네스 전달 실패 6군데 |
| 축 | E 결과 확인 UI | 10 | 6 | 승인 화면이 없음 |
| 축 | F 유지·확장과 법적 구조 | 6 | 3 | 유지 주체·운영 비용 칸이 빔 |
| 축 | G 전달력 | 10 | 7 | 실행 한 줄이 두 갈래 |
| | **축 소계** | **80** | **46** | |
| 게이트 | G1 미래시제 | 5 | **0** | 약속 69건 중 산출물 0인 것 66건 = **95.7%** (>60%) |
| 게이트 | G2 날짜 없는 약속 | 5 | **0** | 누락 **10건** (≥4건) |
| 게이트 | G3 자기충족 지표 | 5 | **0** | 순환 **4건** · 독립성 절차 미문서화 → **B축 50% 상한** |
| 게이트 | G4 버릴 것 부재 | 5 | **5** | 목록 + 트리거 조건 둘 다 있음 |
| | **게이트 소계** | **20** | **5** | |
| | **합계** | **100** | **51** | |

### 게이트 원자료

- **G1**: 약속 총 69건 (step0 7 · step1 5 · step2 9 · step3 6 · step4 4 · step5 6 · step6 6 ·
  step7 6 · step8 3 · step9 2 · step10 3 · step11 4 · step12 5 · harness.md 3).
  실물 확인 — `src/`에는 `src/CLAUDE.md` 하나뿐, `src/matching/` 없음. `data/` 없음.
- **G2**: 10건. 대표 — step2.md:13 「승인 대기」(예정일 없음) · step2.md:190 「미검증으로 completed」
  (언제 되돌아오는지 없음) · step0.md:104 「OCR 도입은 아직 결정되지 않았다」(결정 기한 없음) ·
  「24시간 제약」이 세 번 반복되나 **시작 시각도 마감 달력 시각도 없음**
- **G3**: 순환 4건. 핵심 경로 —
  `step3 requirements.json` → `step10:13 「반드시 파싱된 조건을 먼저 읽고 설계한다」` →
  `step10:22 intended_type 부여` → `step4 build_rubric(동일 requirements)` → 채점 →
  `step11:47 「완벽 2 > 부분 3 > 미스 1 순으로 갈리는지」`.
  **라벨과 채점 대상이 같은 `requirements.json` 하나에 함께 의존한다.**
  `tests/CLAUDE.md:17`은 이 테스트를 「점수 캘리브레이션의 유일한 검증 수단」이라 부른다.
- **G4**: `step12.md:14` 「12-A → 12-B → 12-C, 예산이 모자라면 뒤에서부터 줄인다」 = 목록 + 트리거.
  **이 심사에서 유일하게 만점을 받은 항목이다.**

### veto — 4건 (정지 조건은 0건)

| # | 축 | 주장 | 반증 조건 |
|---|---|---|---|
| V1 | C 실현성 | 데모 전체(요구 ①②⑦)가 **아직 발급 안 된 사람인 access-key** 위에 서 있는데, 키 없이 이미지를 확보하는 경로도 「몇 시까지 안 오면 무엇을 한다」는 **시각이 박힌 대안**도 없다 | (a) 발급 키로 `job-search` 200 응답 로그 또는 `data/.saramin_quota.json` 실호출 기록 / (b) 키 없이 `data/postings/{id}/img_*.png` 2건이 들어오는 경로를 어느 step이 명시 |
| V2 | D 판단 | `step3.md:14-31`의 **VLM 좌표 추출이 이 프로젝트의 유일한 검증 가능한 차별점을 자기 문서의 진단대로 무효화**한다. `src/CLAUDE.md:110`은 「G4가 원문 복붙 안 했다의 기계적 증명」이라 썼는데, 좌표를 VLM이 지어내면 G4는 지어낸 좌표를 검사한다 | step3이 좌표 출처를 **OCR로 바꾸고** `parse_mode`를 결과에 남기도록 개정 / 또는 VLM bbox를 코드가 이미지와 대조하는 절차 추가 |
| V3 | 주최 | 요구 ①② 를 **한 번도 실행 못 한 상태를 `completed`로 기록하라고 명시 지시**한다(`step2.md:190-192`). 하네스 자기 규약(`harness.md:124` 「API 키 필요 → blocked 후 즉시 중단」)을 정면으로 뒤집는다 | 제출물에 실제 공고 이미지 2건 + `requirements.json`이 커밋 이력과 함께 존재 / 또는 키 미발급 시에도 요구 ①을 충족하는 경로를 step으로 명시 |
| V4 | 주최 | 요구 ③(0~100점 + 랭킹)이 **12-B의 무조건 보류 규칙에 통째로 무력화**될 수 있다. `unaddressed`가 **하나라도** 남으면 `rank=None`인데 임계도 상한도 없다 | 실행 결과에서 12명 중 랭킹에 오른 인원수와 `unaddressed_rate` 실측치가 기록되고, 두 공고 모두 랭킹이 정상 출력됨이 확인 |

### 상한 추정 — 합산하지 않는다

| 축 | 지금 | 상한 | 뜻 |
|---|---|---|---|
| A | 7/12 | 9 | |
| B | 7/14 | 9 | 반복 안정성 N·임계가 없어 상단 4점은 실행해도 못 감 |
| C | 8/14 | 10 | 모델·단가·완주 호출수가 없으면 상단 불가 |
| **D** | **8/14** | **6** | **실행하면 내려간다** ← |
| E | 6/10 | 7 | |
| F | 3/6 | 4 | 유지 주체·운영 비용 담당 step이 없어 완주해도 빈 칸 |
| G | 7/10 | 8 | 발표 시나리오·예상 Q&A 지시가 없음 |

**상한이지 예측이 아니다. 실행이 계획을 밑돌면 여기서 더 내려간다.**

### 대각선 해석

- **전 축이 50~70%대다.** 「골고루 중간」은 이 심사에서 가장 나쁜 패턴이다 —
  문서가 아무 데도 뾰족하지 않다는 뜻이다
- **D축이 유일하게 계획 점수 > 상한이다** (8 → 6). 「잘 쓴 계획, 낮은 천장」이고,
  **문서를 더 다듬지 말고 설계를 바꿔야 한다**는 신호다. 원인은 하나 —
  `docs/`의 판단이 하네스까지 전달되지 않았고, 하네스가 그 판단의 **반대**를 지시한다

### 가장 아픈 질문 3개 — 이 심사의 실제 산출물

1. **요구 ②의 정본은 어느 문서인가.** `step3.md:16`은 OpenAI Vision을 부르라 하고,
   `src/CLAUDE.md:161`은 「이미지를 LLM에 보내지 않는다. **좌표의 출처는 OCR 하나뿐이다**」이고,
   `Plans.md:39`는 「Claude 자체 비전으로 수행」이다. 셋 중 지금 유효한 것은 무엇이고,
   나머지 둘은 **언제 무효가 됐으며, 그 무효화를 다른 문서에 반영하는 일은 누가 하기로 돼 있었나.**
2. **키가 마감까지 안 나왔다고 하자. clone한 사람이 「이 사람이 사람인 이미지 공고를 API로
   확보해 이미지에서 파싱했다」를 확인할 파일이 무엇인가.** 이미지는 `.gitignore`로 뺐고
   (`step2.md:73`), 본문 저장은 금지고(`:196`), 공고 ID·URL도 남기지 말라고 했다(`:201`).
3. **한 번 완주에 OpenAI 호출이 몇 회이고, 어느 모델로, 얼마인가.** step 13개 어디에도
   모델명·단가·완주 호출수가 없다. 그 숫자 없이 「$5 예산」을 근거로 **세 개의 결정**을 내렸다 —
   순서 편향 점검 끄기(`step6.md:87`) · 소거 재채점 표본화(`step12.md:118`) · 캐시 강제(`step3.md:109`).

### 주최 적합성 — `judge-ceo`

**조건부.** 점수와 섞지 않는다. veto 2건(V3·V4)이 여기서 나왔다.

---

## 다음 라운드 작업 목록

veto 4건의 반증 조건을 충족시키는 것이 R2의 범위다. 라운드마다
**각 심사위원에게 자기가 쓴 반증 조건이 충족됐는지 검사시킨다** — 새로 채점만 시키면 판정이 표류한다.

---

## R2 — 2026-09-01 03:1x · 변경 이력

루브릭 변경 없음. 봉인 해시 그대로 `1379831dd28ef5bf327d7cd5d720d0f5cbcdca7c466b9310715cf992e0c429ba`.
**기준이 대상을 본 뒤에 바뀌지 않았다** — 바꾼 것은 대상뿐이다.

### veto 대응

| # | 반증 조건 | 무엇을 했나 |
|---|---|---|
| **V1** (C) | (b) 키 없이 이미지가 들어오는 경로를 어느 step이 명시 | **`docs/SCHEDULE.md` 신설** — §0 기준 시각(마감 2026-09-01 18:00 KST), §1 시각별 계획 + 「17:00 이후 코드 금지」, **§2 이미지 확보 분기**(06:00 판단 시점 · 경로 A/B · 경로 B에서 잃는 것), §3 `provenance.json` |
| **V2** (D) | step3이 좌표 출처를 OCR로 바꾸고 `parse_mode`를 결과에 남기도록 개정 | **`step3.md` 전면 재작성.** `parser/vision.py` 폐기, 상단에 폐기 기록. 3-A OCR → 3-B 레이아웃(코드) → 3-C 헤더 역할(LLM, **문자열만**) → 3-D 사다리 → 3-E `ParseReport.parse_mode`. **`grep -rn "image_url\|base64" src/matching/parser`가 비어야 한다**를 AC로 |
| **V3** (CEO) | 키 미발급 시에도 요구 ①을 충족하는 경로를 step으로 명시 | **`step2.md` status 규칙 재설계** — step2는 코드, step3은 데이터. **`step3.md`에 전제조건 게이트**(이미지 0장 → `blocked` 후 중단, `harness.md:124` 준수). `index.json`에 **`requirement_status`** 필드 신설 — `status: completed`가 `req1: verified`를 못 덮는다 |
| **V4** (CEO) | 두 공고 모두 랭킹이 정상 출력됨이 확인 | **12-B 임계 도입.** `unaddressed_tolerance=0.15` · `ledger_degraded_ratio=0.5`. **`rank=None`을 없앴다** — 미처리는 우리 대장의 결함이지 지원자의 결격이 아니다. AC에 「두 공고 모두 6명이 랭킹에」 검사 추가 |

### 게이트 대응

| | 무엇을 했나 |
|---|---|
| **G1** 95.7% | 문서 3개 실물 생성(`SCHEDULE` · `COST_BUDGET` · `OCR_EVIDENCE`) + `step13.md`. **비율은 R2 심사에서 다시 센다** — 근본 해소는 실행뿐이다 |
| **G2** 10건 | `docs/SCHEDULE.md`가 **마감·판단 시점·push 마감**을 달력 시각으로 못박음. step0의 「OCR 도입 미결정」은 결정 완료로 취소선 |
| **G3** 순환 4건 | 순환을 **못 끊는다는 것을 인정**하고 ① 의존 경로를 좁혔다(step10이 `weight`·`criterion_id`·`rubric.json`을 **안 읽는다**, `read_fields`에 기록) ② **기계 검사**(`design_note` grep + `read_fields` assert) ③ **홀드아웃 조건 2개** ④ `tests/CLAUDE.md`를 **1차/2차 지표로 재편** — 「유일한 검증 수단」 문구 삭제, 직군 교차를 1차로 |
| **G4** 만점 | 유지. `docs/COST_BUDGET.md` §5에 **버리는 순서 5단계**로 확장(3번째 = 「3번째 심사위원」) |

### 축별 감점 대응

| 축 | R1이 지적한 것 | 대응 |
|---|---|---|
| A | 요구 ①의 AC 부재 · ②가 폐기 경로 지시 | `requirement_status` · step3 재작성 |
| B | 반복 안정성에 숫자 0 | **N=5 · σ≤0.5** (`step6.md` · `tests/CLAUDE.md`). 임의값임을 명시. `TRADEOFFS.md:100`의 철회 조건이 이제 발동 가능 |
| C | 비용·시간 숫자 0건 | **`docs/COST_BUDGET.md` 신설** — 완주 ≈117회(최악 134), 재실행 비용표, 토큰 추정, §4 실측 칸. 모델·단가는 `settings` 외부화. `step6.md`에 `temperature=0`·모델 핀·`prompt_sha256` |
| D | 하네스가 아키텍처 문서의 금지를 지시 3건 | 3건 모두 해소 + `docs/SARAMIN_API.md`를 step2 읽기 목록에 추가(모순이 살아남은 **메커니즘**) + `src/CLAUDE.md`의 EasyOCR·글자크기 규칙 개정 |
| E | 첫 화면 없음 · 승인 경로 없음 | `step9.md`에 **0단 시작 화면 + 0.5단 승인 화면**, `step8.md`에 **`POST /prepare`·`POST /approve`·`GET /image/{id}`**. 배지 개수 규칙(행당 2개) |
| F | 유지 주체·운영 비용 담당 step 부재 | **`step13.md` 신설** — 13-B `docs/OPERATIONS.md` |
| G | 실행 한 줄이 두 갈래 | **`python run.py` 하나로 통일.** `step0.md` AC를 `pip install -e .` + `python -m matching --help`로. 13-C `docs/DEMO.md`에 발표 시나리오·예상 Q&A·**못 하는 것 목록** |

### 이번 라운드에 추가로 반영된 것 (심사와 무관)

- **대상 공고 2개 확정** (사용자 지정) — KT NW인프라운용 / 넥슨 게임 프로그래머. `Plans.md` Phase 1.5
- **`step3.md` 3-B′ 신설** — KT가 3열 표라 셀 소속 문제가 실물이 됐다. 표를 표로 파싱하지 않고
  **대상 직무의 y 구간만** 자른다. 표 밖 공통 섹션(「지원자격」)은 살린다
- **`step10.md`** — 목업 이력서를 **실제 지원서 형식을 참고해** 쓴다(사용자 지시).
  실존 인물·문장 복사는 금지 그대로
- **`step9.md`** — 화면 구현은 사용자 담당. 이 step은 **데이터 계약**으로 성격 변경

### R2 판정 — 61 / 100 (R1 51에서 +10)

| 축 | R1 | **R2** | 상한 | |
|---|---|---|---|---|
| A 요구사항 충족 | 7 | **9** | 11 | |
| B 반증 가능성 | 8→7 | **10→7** | 11 | G3 0점 → 50% 상한 유지 |
| C 24시간 실현성 | 8 | **10** | 11 | **V1 철회** |
| D 본인의 판단 | 8 | **11** | 12 | **V2 철회.** 상한이 6→12로 뒤집혔다 |
| E 결과 확인 UI | 6 | **7** | 8 | |
| F 유지·법적 구조 | 3 | **4** | 5 | |
| G 전달력 | 7 | **8** | 9 | |
| **축 소계** | 46 | **56** | / 80 | |
| G1 미래시제 | 0 | **0** | 5 | 95.7% → **95.9%.** 제자리 |
| G2 날짜 없는 약속 | 0 | **0** | 5 | 10건 → **9건.** 감소가 아니라 **교체** |
| G3 자기충족 | 0 | **0** | 5 | 순환 잔존 |
| G4 버릴 것 | 5 | **5** | 5 | |
| **합계** | **51** | **61** | /100 | |

**veto: 축 심사위원 7명 전원 0건.** CEO만 V3·V4를 **조건부 철회**하고 **새 veto V5**를 냈다.

### R2에서 새로 드러난 것

| | 무엇 | 처리 |
|---|---|---|
| **V5** (CEO) | 키 없는 경로에서 **이미지를 놓고 `provenance.json`을 만드는 담당이 없다.** SCHEDULE에만 서술되고 step2는 코드만, step3은 0장이면 blocked | **step2에 `provenance.py` + `python -m matching acquire` 신설.** step2 말미에 「누가 무엇을 하나」 표 — **「사람이 한다」를 명시**. step3 전제조건·AC가 검사 |
| A·F의 Q | 같은 구멍을 독립적으로 지목 | 〃 |
| **경로 버그** (G) | step7은 `data/runs/`, step 8·12·13은 `out/runs/` → **step13이 자기 전제조건에 영구히 걸림.** 게다가 `out/`은 `.gitignore`라 결과가 커밋 안 됨 | `data/runs/`로 통일 |
| **N=5 vs 문헌 11회** (B) | 우리가 인용한 `RUBRIC_DESIGN.md:109`가 「95% 안정에 **11회 이상**」인데 N을 5로 적었다 | **N=11로 고침.** 예산이 모자라면 N을 깎지 말고 테스트를 **버린다** |
| **TRADEOFFS E-1 미결** (D) | OCR 결정이 확정됐는데 §E-1은 「아직 결정 안 됨」이고 step3이 그걸 근거로 인용 | 결정 블록 추가. **쟁점 원문은 남김** |
| **AC가 0건일 때 통과** (C) | step3 AC의 glob이 비면 루프가 안 돌아 조용히 통과 | `assert len(reqs)==2`를 **첫 줄로** |
| **날짜 없는 약속 9건** (G) | 「나중에 고친다」에 시각이 없음. **일정표를 만드니 어디가 빈지 보였다** | `SCHEDULE.md` §1에 step 14개 전부 · **§5 「나중에 고친다」의 시각** 신설 |

### 대각선 — R1의 진단이 뒤집혔다

**R1: D축만 계획 점수(8) > 상한(6) — 「잘 쓴 계획, 낮은 천장」.**
**R2: D축 11, 상한 12.** 천장이 6에서 12로 올라갔다. 실행하면 내려가던 축이 올라가는 축이 됐다.

원인은 하나다 — R1의 상한을 누른 것은 **문서의 질이 아니라 하네스가 아키텍처 문서의
반대를 지시하던 3건**이었고, 그게 사라졌다.

**남은 문제는 게이트 셋이 전부 0이라는 것이다.** 그리고 셋 다 **실행해야만 움직인다**:
G1은 코드가 생겨야, G2는 실행이 시각표를 검증해야, G3은 외부 정답 데이터가 있어야.
**계획을 더 다듬어서 오르는 점수가 아니다.**

---

## R3 — 2026-09-01 02:4x

**부분 라운드다.** veto는 V5(CEO) 하나뿐이었고 바뀐 자리를 담당한 **4명만** 불렀다
(A·B·G·CEO). C·D·E·F는 R2 점수를 유지한다. 루브릭 해시 불변.

### 스코어카드 — 72 / 100 (R2 61에서 +11)

| 축 | R1 | R2 | **R3** | 상한 |
|---|---|---|---|---|
| A 요구사항 충족 | 7 | 9 | **10** | 11 |
| B 반증 가능성 | 7 | 7 | **11** | 12 |
| C 24시간 실현성 | 8 | 10 | 10 *(유지)* | 11 |
| D 본인의 판단 | 8 | 11 | 11 *(유지)* | 12 |
| E 결과 확인 UI | 6 | 7 | 7 *(유지)* | 8 |
| F 유지·법적 구조 | 3 | 4 | 4 *(유지)* | 5 |
| G 전달력 | 7 | 8 | **9** | 10 |
| **축 소계** | 46 | 56 | **62 / 80** | |
| G1 미래시제 | 0 | 0 | **0** | 87.6% (>60%) |
| G2 날짜 없는 약속 | 0 | 0 | **2.5** | **9건 → 1건** |
| G3 자기충족 | 0 | 0 | **2.5** | **0점 → 「절반」** |
| G4 버릴 것 | 5 | 5 | 5 | |
| **합계** | **51** | **61** | **72** | |

**B축의 50% 상한이 풀렸다** — G3가 0점에서 「절반」으로 올라갔기 때문이다.
`judge-quant`의 판정: 「절차가 산문이 아니라 **실행 가능한 검사 3개**로 AC에 박혀 있다」
+ 「0점 칸을 문자 그대로 적용하면 외부 정답 데이터가 없는 24시간 과제에서는 완화를 얼마나
했든 결과가 같아져 **등급이 정보를 잃는다**」.

### R3에서 고친 것

| 출처 | 무엇 | 대응 |
|---|---|---|
| **V5 → 철회** | `provenance.json`을 만드는 담당이 없다 | `step2`에 `provenance.py` + `acquire` CLI + **「누가 무엇을 하나」 표**(사람이 하는 일을 사람이 한다고 적었다) |
| **V6** (신규, CEO) | `.gitignore` 패턴이 안 박혀 있어 `data/` 통째 무시 시 증거가 전부 사라짐 | `step0`에 **파일 단위 패턴 3줄** + 확대 해석 경고. `step13` AC가 **`git ls-files`로 tracked 확인** |
| **CEO의 질문** | 과제문이 「컴팩트하게」·「전체 흐름 먼저」인데 첫 완주가 11:30 | **`index.json` 배열 순서 변경** — step 10을 step 4 앞으로. **첫 완주 11:30 → 09:30** |
| **B의 P4** | `step11`이 실측으로 임계를 되맞춘다 = 판정선을 판정 대상에서 뽑는다 | 그 문장 **취소**. 실측만 적고 임계 조정은 이관 |
| **B의 질문** | 홀드아웃 임계 「분산 > 0」은 **채점 노이즈만으로 통과**한다 | `sd_holdout > 2 × sd_noise` (σ는 step 6 실측) |
| **B의 P2** | `kind`가 배점 계급 아닌가 | `weight = 층총합 × kind상대비 / 항목수`라 **`kind`만으로 계산 불가**. 남는 위험도 자인 |
| **B의 P5** | `read_fields` assert는 **자기신고**를 본다 | 방어 3층 표. **「순서가 진짜 방어, 나머지는 보조」** |
| **A의 지적** | 요구 ①의 AC가 산문뿐 | `.saramin_quota.json` **실호출 기록과 기록된 상태가 어긋나면 AC가 깨진다** |
| **A의 질문** | 13-D의 운명이 어느 문서에도 없다 | **13-A·13-D는 절대 안 버린다**(합쳐 20분). 13-C를 버리면 표는 `index.json`으로 |
| **G의 질문** | `data/runs` vs `out/runs` — step13이 자기 전제조건에 영구히 걸림 | 경로 통일 |

### 순서 재배치가 두 문제를 동시에 풀었다

step 10을 step 4 앞으로 옮기니:

1. **첫 완주가 2시간 당겨졌다** — 과제문의 「전체 흐름이 처음부터 끝까지 이어지도록」
2. **G3 독립성이 절차에서 구조로 바뀌었다** — 이력서를 쓸 때 `rubric.json`이 **아직 없다.**
   「배점을 읽지 마라」가 규칙이 아니라 **사실**이 된다

R1의 `judge-engineer`와 R3의 `judge-ceo`가 두 라운드 떨어져 **독립적으로 같은 것**을
지적했다 — 「데이터가 맨 뒤에 있어 마감 압박 때 잘리는 쪽이 정확히 제출 필수물」.
